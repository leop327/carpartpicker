/** Re-exports for callers expecting `types/index` (Next-style imports). */
export type {
  BuildSelection,
  CarModel,
  Colour,
  Drivetrain,
  Figures,
  FiguresDelta,
  FiguresSource,
  Market,
  Mod,
  ModCategory,
  ModCategoryId,
  SpecChoice,
  SpecOptionGroup,
  StagePreset,
  UkMotStatus,
} from './catalog'
